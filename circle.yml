machine:
  node:
    version: 6.2.0
general:
  branches:
    ignore:
      - gh-pages
deployment:
  static_development:
    owner: lmorchard
    branch:
      - development
    commands:
      - git config --global user.email "me@lmorchard.com"
      - git config --global user.name "Les Orchard"
      - npm run release:dev
